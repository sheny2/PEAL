
R version 4.3.1 Patched (2023-07-19 r84711) -- "Beagle Scouts"
Copyright (C) 2023 The R Foundation for Statistical Computing
Platform: x86_64-conda-linux-gnu (64-bit)

R is free software and comes with ABSOLUTELY NO WARRANTY.
You are welcome to redistribute it under certain conditions.
Type 'license()' or 'licence()' for distribution details.

  Natural language support but running in an English locale

R is a collaborative project with many contributors.
Type 'contributors()' for more information and
'citation()' on how to cite R or R packages in publications.

Type 'demo()' for some demos, 'help()' for on-line help, or
'help.start()' for an HTML browser interface to help.
Type 'q()' to quit R.

> suppressPackageStartupMessages({
+   library(doParallel)
+   library(doRNG)
+   library(foreach)
+   library(data.table)
+   library(dplyr)
+   library(MASS)
+   library(reshape2)
+   library(ggplot2)
+   library(Matrix)    # for nearPD
+   library(gridExtra) # for grid.arrange
+   library(tidyr)     # for pivot_wider
+   library(mice)      # for MICE imputation
+ })
> 
> ## ------------------------------------------------
> ## 1. Global settings and true parameters
> ## ------------------------------------------------
> 
> source("MV-PEAL_Pattern.R")
> # source("MV-PEAL.R")
> 
> H      <- 5    # number of sites
> px     <- 4
> p_bin  <- 2
> p_cont <- px - p_bin
> py     <- 3   # number of outcomes
> 
> X_cols <- paste0("X", 1:px)
> Y_cols <- paste0("Y", 1:py)
> 
> beta_true <- matrix(runif(px * py, -10, 10), nrow = px, ncol = py)
> 
> sigma_u_true      <- 0.5
> sigma_v_hosp_true <- runif(H, min = 0.5, max = 1)   # fixed across sims
> sigma_e_true      <- 0.3
> 
> sigma_e_vec <- rep(sigma_e_true, py)
> De          <- diag(sigma_e_vec, py)
> 
> # True parameter vectors for later comparison (corr part is below)
> true_beta_vec     <- c(beta_true)
> true_sigmaRE_vec  <- c(sigma_u_true, sigma_v_hosp_true)  # length H+1
> true_sigma_e      <- sigma_e_true
> 
> par_names_beta    <- paste0("Beta", 1:(px * py))
> par_names_sigmaRE <- c("sigma_u", paste0("sigma_v_", 1:H))
> par_name_sigma_e  <- "sigma_e"
> corr_param_names  <- paste0("rho_", rep(1:py, each = py), "_", rep(1:py, times = py))
> 
> ## ------------------------------------------------
> ## 1a. FIXED correlation structure (same for all scenarios)
> ## ------------------------------------------------
> 
> rho12_fixed <- -0.2
> rho13_fixed <- 0.4
> rho23_fixed <- 0.8
> 
> corr_mat_true <- matrix(c(
+   1,           rho12_fixed, rho13_fixed,
+   rho12_fixed, 1,           rho23_fixed,
+   rho13_fixed, rho23_fixed, 1
+ ), nrow = py, byrow = TRUE)
> 
> # Ensure SPD
> if (min(eigen(corr_mat_true, symmetric = TRUE, only.values = TRUE)$values) <= 0) {
+   corr_mat_true <- as.matrix(nearPD(corr_mat_true, corr = TRUE)$mat)
+ }
> 
> Sigma_e       <- De %*% corr_mat_true %*% De  # fixed residual covariance
> true_corr_vec <- as.vector(corr_mat_true)
> 
> ## ------------------------------------------------
> ## 2. Helper: build Z list (site-wise random intercepts)
> ##    (kept in case needed by MV-PEAL code)
> ## ------------------------------------------------
> build_Z_list <- function(dat, H) {
+   Z <- vector("list", H)
+   for (i in 1:H) {
+     count_mat <- dat %>%
+       dplyr::filter(site == i) %>%
+       dplyr::group_by(site, patient) %>%
+       dplyr::summarise(n_hi = n(), .groups = "drop")
+     Z[[i]] <- generate_Zhv_matrix(count_mat)
+   }
+   Z
+ }
> 
> ## ------------------------------------------------
> ## 3. Single replicate DGP: MAR-masked data
> ##    (takes Sigma_e and p1_base, p2_base, p3_base)
> ## ------------------------------------------------
> simulate_one <- function(seed, Sigma_e, p1_base, p2_base, p3_base) {
+   set.seed(seed)
+   
+   # --- DGP ---
+   m_hosp <- sample(75:100, H, replace = TRUE)
+   nn     <- rep(m_hosp, times = 1)
+   id.hosp <- rep(seq_len(H), times = m_hosp)
+   id.pat  <- sequence(nn)
+   n_visits <- sample(1:50, sum(nn), replace = TRUE)
+   
+   id.visit         <- sequence(n_visits)
+   id.hosp.expanded <- rep(id.hosp, times = n_visits)
+   id.pat.expanded  <- rep(id.pat,  times = n_visits)
+   
+   # Random effects
+   u_h  <- rnorm(H, mean = 0, sd = sigma_u_true)
+   v_hi <- rnorm(sum(nn), mean = 0, sd = rep(sigma_v_hosp_true, times = m_hosp))
+   
+   u_h_patient   <- rep(u_h, times = m_hosp)
+   u_h_expanded  <- rep(u_h_patient, times = n_visits)
+   v_hi_expanded <- rep(v_hi, times = n_visits)
+   
+   # Covariates
+   Nobs   <- sum(n_visits)
+   X_bin  <- matrix(rbinom(Nobs * p_bin, 1, 0.3), nrow = Nobs, ncol = p_bin)
+   X_cont <- matrix(rnorm(Nobs * p_cont, 0, 1),   nrow = Nobs, ncol = p_cont)
+   X_hij  <- cbind(X_bin, X_cont)
+   
+   # Residuals with given Sigma_e
+   epsilon_hij <- MASS::mvrnorm(n = Nobs, mu = rep(0, py), Sigma = Sigma_e)
+   
+   # Outcomes
+   Y_hij <- matrix(0, nrow = Nobs, ncol = py)
+   for (k in 1:py) {
+     Y_hij[, k] <- X_hij %*% beta_true[, k] + u_h_expanded + v_hi_expanded + epsilon_hij[, k]
+   }
+   
+   three_lvl_dat <- data.table(
+     site    = id.hosp.expanded,
+     patient = id.pat.expanded,
+     visit   = id.visit,
+     X_hij, Y_hij
+   ) %>% as.data.frame()
+   
+   data.table::setnames(three_lvl_dat,
+                        c("site","patient","visit", paste0("X",1:px), paste0("Y",1:py)))
+   
+   visit_count <- three_lvl_dat %>%
+     group_by(site, patient) %>%
+     summarise(total_visits = n(), .groups = "drop")
+   
+   rearranged_data <- merge(three_lvl_dat, visit_count, by = c("site","patient")) %>%
+     arrange(site, total_visits, patient) %>%
+     mutate(site = factor(site))
+   
+   ## --- MAR missingness (using p1_base, p2_base, p3_base) ---
+   rearranged_data_with_missing <- rearranged_data
+   n <- nrow(rearranged_data)
+   
+   # Y3 depends on Y1,Y2 observed
+   Y3_miss_prob <- ifelse(!is.na(rearranged_data$Y1) & !is.na(rearranged_data$Y2),
+                          p3_base + 0.01,
+                          p3_base - 0.01)
+   Y3_miss_prob <- pmin(pmax(Y3_miss_prob, 0), 1)
+   Y3_missing   <- rbinom(n, 1, Y3_miss_prob) == 1
+   rearranged_data_with_missing$Y3[Y3_missing] <- NA
+   
+   # Y1, Y2 depend on Y3 observed
+   Y1_miss_prob <- ifelse(!is.na(rearranged_data_with_missing$Y3),
+                          p1_base + 0.01,
+                          p1_base - 0.01)
+   Y2_miss_prob <- ifelse(!is.na(rearranged_data_with_missing$Y3),
+                          p2_base + 0.01,
+                          p2_base - 0.01)
+   Y1_miss_prob <- pmin(pmax(Y1_miss_prob, 0), 1)
+   Y2_miss_prob <- pmin(pmax(Y2_miss_prob, 0), 1)
+   
+   Y1_missing <- rbinom(n, 1, Y1_miss_prob) == 1
+   Y2_missing <- rbinom(n, 1, Y2_miss_prob) == 1
+   
+   rearranged_data_with_missing$Y1[Y1_missing] <- NA
+   rearranged_data_with_missing$Y2[Y2_missing] <- NA
+   
+   # Ensure no row with all Y's missing
+   all_y_missing <- apply(
+     rearranged_data_with_missing[, paste0("Y",1:py), drop = FALSE],
+     1, function(x) all(is.na(x))
+   )
+   if (any(all_y_missing)) {
+     for (i in which(all_y_missing)) {
+       y_to_keep <- sample(paste0("Y",1:py), 1)
+       rearranged_data_with_missing[i, y_to_keep] <-
+         rearranged_data[i, y_to_keep]
+     }
+   }
+   
+   list(
+     full   = rearranged_data,
+     miss   = rearranged_data_with_missing
+   )
+ }
> 
> ## ------------------------------------------------
> ## 4. MICE imputation helper (site-wise)
> ## ------------------------------------------------
> 
> impute_mice_by_site <- function(dat, X_cols, Y_cols,
+                                 m = 1, maxit = 5,
+                                 base_seed = 12345) {
+   dat_imputed <- dat
+   
+   # Work site by site to mimic local imputation then combine
+   sites <- unique(dat$site)
+   for (s in sites) {
+     idx_s   <- which(dat$site == s)
+     subdat  <- dat[idx_s, ]
+     
+     # Only impute if there is missingness in Y's
+     y_mat <- as.matrix(subdat[, Y_cols, drop = FALSE])
+     if (all(!is.na(y_mat))) {
+       next
+     }
+     
+     # MICE on X+Y only; site/patient/visit left untouched
+     mice_dat <- subdat[, c(X_cols, Y_cols), drop = FALSE]
+     
+     meth <- rep("", ncol(mice_dat))
+     names(meth) <- colnames(mice_dat)
+     # Impute only outcomes; X's have no missing and are predictors
+     meth[Y_cols] <- "pmm"
+     
+     mids <- mice(
+       mice_dat,
+       m          = m,
+       maxit      = maxit,
+       method     = meth,
+       printFlag  = FALSE,
+       seed       = base_seed + as.integer(s)
+     )
+     
+     comp <- complete(mids, 1)
+     
+     # Replace only Y's in subdat with imputed values
+     subdat[, Y_cols] <- comp[, Y_cols]
+     
+     dat_imputed[idx_s, ] <- subdat
+   }
+   
+   dat_imputed
+ }
> 
> ## ------------------------------------------------
> ## 5. Fit helpers and packers
> ## ------------------------------------------------
> 
> pack_fit <- function(fit, py, H) {
+   if (is.null(fit)) {
+     return(list(
+       b        = rep(NA_real_, px * py),
+       b_sd      = rep(NA_real_, px * py),
+       sigmaRE  = rep(NA_real_, H + 1),
+       sigma_e  = NA_real_,
+       corr_vec = rep(NA_real_, py * py)
+     ))
+   }
+   b_hat       <- as.numeric(fit$b)
+   b_hat_sd       <- as.numeric(fit$b.sd)
+   sigmaRE_hat <- sqrt(fit$theta * fit$s2)  # H+1 random-effect SDs
+   sigmae_hat  <- sqrt(fit$s2)
+   
+   if (!is.null(fit$Corr)) {
+     Corr_hat     <- fit$Corr
+     corr_vec_hat <- as.vector(Corr_hat)
+   } else {
+     corr_vec_hat <- rep(NA_real_, py * py)
+   }
+   
+   list(
+     b        = b_hat,
+     b_sd     = b_hat_sd,
+     sigmaRE  = sigmaRE_hat,
+     sigma_e  = sigmae_hat,
+     corr_vec = corr_vec_hat
+   )
+ }
> 
> ## ------------------------------------------------
> ## 6. Model fitting: OBS_UN (patterns), CC_UN, MICE_UN, FULL_UN (all unstructured)
> ## ------------------------------------------------
> 
> fit_all_models <- function(dat_full, dat_miss) {
+   ## 6.1 OBS_UN: pattern-based MV-PEAL using incomplete data
+   fit_obs_un <- tryCatch(
+     peal.fit.RI_mv_patterns(
+       data         = dat_miss,
+       X_cols       = X_cols,
+       Y_cols       = Y_cols,
+       site_col     = "site",
+       patient_col  = "patient",
+       corstr       = "unstructured",
+       reml         = TRUE,
+       verbose      = FALSE
+     ),
+     error = function(e) NULL
+   )
+   res_obs_un <- pack_fit(fit_obs_un, py, H)
+   
+   ## 6.2 CC_UN: complete-case analysis
+   dat_cc <- dat_miss %>%
+     dplyr::filter(complete.cases(dplyr::select(., dplyr::all_of(Y_cols))))
+   
+   fit_cc_un <- NULL
+   if (nrow(dat_cc) > 0) {
+     fit_cc_un <- tryCatch(
+       peal.fit.RI_mv_patterns(
+         data         = dat_cc,
+         X_cols       = X_cols,
+         Y_cols       = Y_cols,
+         site_col     = "site",
+         patient_col  = "patient",
+         corstr       = "unstructured",
+         reml         = TRUE,
+         verbose      = FALSE
+       ),
+       error = function(e) NULL
+     )
+   }
+   res_cc_un <- pack_fit(fit_cc_un, py, H)
+   
+   ## 6.3 MICE_UN: site-wise MICE
+   dat_mice <- impute_mice_by_site(dat_miss, X_cols, Y_cols)
+   
+   fit_mice_un <- tryCatch(
+     peal.fit.RI_mv_patterns(
+       data         = dat_mice,
+       X_cols       = X_cols,
+       Y_cols       = Y_cols,
+       site_col     = "site",
+       patient_col  = "patient",
+       corstr       = "unstructured",
+       reml         = TRUE,
+       verbose      = FALSE
+     ),
+     error = function(e) NULL
+   )
+   res_mice_un <- pack_fit(fit_mice_un, py, H)
+   
+   ## 6.4 FULL_UN: full data (no missing)
+   fit_full_un <- tryCatch(
+     peal.fit.RI_mv_patterns(
+       data         = dat_full,
+       X_cols       = X_cols,
+       Y_cols       = Y_cols,
+       site_col     = "site",
+       patient_col  = "patient",
+       corstr       = "unstructured",
+       reml         = TRUE,
+       verbose      = FALSE
+     ),
+     error = function(e) NULL
+   )
+   res_full_un <- pack_fit(fit_full_un, py, H)
+   
+   list(
+     OBS_UN  = res_obs_un,
+     CC_UN   = res_cc_un,
+     MICE_UN = res_mice_un,
+     FULL_UN = res_full_un
+   )
+ }
> 
> ## ------------------------------------------------
> ## 7. One replicate wrapper (takes Sigma_e & missingness bases)
> ## ------------------------------------------------
> 
> run_one_rep <- function(seed, Sigma_e, p1_base, p2_base, p3_base) {
+   sim <- simulate_one(seed, Sigma_e, p1_base, p2_base, p3_base)
+   res <- fit_all_models(sim$full, sim$miss)
+   c(res)
+ }
> 
> ## ------------------------------------------------
> ## 8. Helper to init matrices and long-format helper
> ## ------------------------------------------------
> 
> init_mat <- function(nr, N) {
+   matrix(NA_real_, nrow = nr, ncol = N,
+          dimnames = list(NULL, paste0("sim", 1:N)))
+ }
> 
> mk_long <- function(mat, par_names, model_label, value_nm = "Estimate") {
+   df <- reshape2::melt(as.data.frame(mat), value.name = value_nm)
+   names(df) <- c("Simulation", value_nm)
+   df$Parameter <- rep(par_names, ncol(mat))
+   df$Model <- model_label
+   df
+ }
> 
> ## ------------------------------------------------
> ## 9. BIG DRIVER: run whole pipeline for one missingness setting
> ## ------------------------------------------------
> 
> run_sim_for_missing <- function(p1_base, p2_base, p3_base,
+                                 N = 200,
+                                 label = NULL) {
+   if (is.null(label)) {
+     label <- paste0("miss_",
+                     paste(sprintf("%0.2f", c(p1_base, p2_base, p3_base)),
+                           collapse = "_"))
+   }
+   cat("=== Running simulation for", label, "===\n")
+   cat("   p1_base =", p1_base,
+       "p2_base =", p2_base,
+       "p3_base =", p3_base, "\n")
+   cat("   Fixed rho = (", rho12_fixed, ",", rho13_fixed, ",", rho23_fixed, ")\n")
+   
+   model_names <- c("OBS_UN", "CC_UN", "MICE_UN", "FULL_UN")
+   
+   ## ------------------------------------------------
+   ## Parallel simulation: use SLURM cpus-per-task
+   ## ------------------------------------------------
+   num_cores <- as.integer(Sys.getenv("SLURM_CPUS_PER_TASK", "1"))
+   if (is.na(num_cores) || num_cores < 1) num_cores <- 1
+   
+   cat("Using", num_cores, "cores for foreach.\n")
+   
+   cl <- makeCluster(num_cores)
+   registerDoParallel(cl)
+   
+   # Seed base can depend on label if you want, but constant is fine per setting
+   registerDoRNG(627)
+   
+   results <- foreach(
+     k = 1:N,
+     .packages = c("data.table","dplyr","MASS","Matrix","reshape2","mice"),
+     .export   = c(
+       "run_one_rep",
+       "simulate_one",
+       "fit_all_models",
+       "pack_fit",
+       "impute_mice_by_site",
+       "H", "px", "p_bin", "p_cont", "py",
+       "X_cols", "Y_cols",
+       "sigma_u_true", "sigma_v_hosp_true",
+       "beta_true", "De", "Sigma_e",
+       "rho12_fixed", "rho13_fixed", "rho23_fixed"
+     )
+   ) %dopar% {
+     # each worker re-sources the PEAL code
+     source("MV-PEAL_Pattern.R")
+     # source("MV-PEAL.R")
+     
+     run_one_rep(sample(1:1e6, 1), Sigma_e, p1_base, p2_base, p3_base)
+   }
+   
+   stopCluster(cl)
+   
+   # Save raw results list for this missingness setting
+   saveRDS(results, file = paste0("Simulation_Results_", label, ".rds"))
+   
+   ## ------------------------------------------------
+   ## Collect results into matrices
+   ## ------------------------------------------------
+   store <- lapply(model_names, function(nm) {
+     list(
+       beta    = init_mat(px * py, N),
+       sigmaRE = init_mat(H + 1, N),
+       sigma_e = init_mat(1, N),
+       corr    = init_mat(py * py, N)
+     )
+   })
+   names(store) <- model_names
+   
+   for (k in seq_along(results)) {
+     resk <- results[[k]]
+     for (nm in model_names) {
+       store[[nm]]$beta[,   k] <- resk[[nm]]$b
+       store[[nm]]$sigmaRE[,k] <- resk[[nm]]$sigmaRE
+       store[[nm]]$sigma_e[,k] <- resk[[nm]]$sigma_e
+       store[[nm]]$corr[,  k]  <- resk[[nm]]$corr_vec
+     }
+   }
+   
+   ## ------------------------------------------------
+   ## Build long-format data frames (for later bias/variance/coverage work)
+   ## ------------------------------------------------
+   # 1) beta
+   beta_long <- do.call(rbind, lapply(names(store), function(nm) {
+     mk_long(store[[nm]]$beta, par_names_beta, nm, "Estimate")
+   }))
+   beta_long$True <- rep(true_beta_vec,
+                         times = ncol(store[[1]]$beta) * length(store))
+   
+   # 2) sigmaRE
+   sigmaRE_long <- do.call(rbind, lapply(names(store), function(nm) {
+     mk_long(store[[nm]]$sigmaRE, par_names_sigmaRE, nm, "Estimate")
+   }))
+   sigmaRE_long$True <- rep(true_sigmaRE_vec,
+                            times = ncol(store[[1]]$sigmaRE) * length(store))
+   
+   # 3) sigma_e
+   sigmae_long <- do.call(rbind, lapply(names(store), function(nm) {
+     mk_long(store[[nm]]$sigma_e, par_name_sigma_e, nm, "Estimate")
+   }))
+   sigmae_long$True <- rep(true_sigma_e,
+                           times = ncol(store[[1]]$sigma_e) * length(store))
+   
+   # 4) corr
+   corr_long <- do.call(rbind, lapply(names(store), function(nm) {
+     mk_long(store[[nm]]$corr, corr_param_names, nm, "Estimate")
+   }))
+   corr_long$True <- rep(true_corr_vec,
+                         times = ncol(store[[1]]$corr) * length(store))
+   
+   ## ------------------------------------------------
+   ## Optional bias plots (kept; no RE computed)
+   ## ------------------------------------------------
+   beta_long$Parameter <- factor(beta_long$Parameter,
+                                 levels = paste0("Beta", 1:(px*py)))
+   
+   p_beta_bias <- beta_long %>%
+     mutate(Bias = Estimate - True) %>%
+     ggplot(aes(x = Parameter, y = Bias)) +
+     geom_jitter(alpha = 0.1, width = 0.15, height = 0, size = 0.3) +
+     geom_boxplot(fill = "lightblue", alpha = 0.6, outlier.shape = NA) +
+     facet_wrap(~ Model, ncol = 2) +
+     theme_bw() + 
+     theme(axis.text.x = element_text(angle = 45, hjust = 1)) +
+     geom_hline(yintercept = 0, color = "red", linetype = "dashed", linewidth = 0.7) +
+     ggtitle(paste("Fixed Effects Bias -", label))
+   
+   p_sigmaRE_bias <- sigmaRE_long %>%
+     mutate(Bias = Estimate - True) %>%
+     ggplot(aes(x = Parameter, y = Bias)) +
+     geom_jitter(alpha = 0.1, width = 0.15, height = 0, size = 0.3) +
+     geom_boxplot(fill = "lightgreen", alpha = 0.6, outlier.shape = NA) +
+     facet_wrap(~ Model, ncol = 2) +
+     theme_bw() + 
+     theme(axis.text.x = element_text(angle = 45, hjust = 1)) +
+     geom_hline(yintercept = 0, color = "red", linetype = "dashed", linewidth = 0.7) +
+     ggtitle(paste("Random-effect SD Bias -", label))
+   
+   p_sigmae_bias <- sigmae_long %>%
+     mutate(Bias = Estimate - True) %>%
+     ggplot(aes(x = Model, y = Bias)) +
+     geom_boxplot(fill = "orange", alpha = 0.6) +
+     theme_bw() +
+     geom_hline(yintercept = 0, color = "red", linetype = "dashed", linewidth = 0.7) +
+     ggtitle(paste("Residual SD Bias -", label))
+   
+   corr_long_offdiag <- corr_long %>%
+     mutate(i = as.integer(substr(Parameter, 5, 5)),
+            j = as.integer(substr(Parameter, 7, 7))) %>%
+     filter(i != j)
+   corr_long_offdiag$Parameter <- factor(corr_long_offdiag$Parameter,
+                                         levels = unique(corr_long_offdiag$Parameter))
+   
+   p_corr_bias <- corr_long_offdiag %>%
+     filter(Parameter %in% unique(corr_long_offdiag$Parameter)[c(1,2,4)]) %>%
+     mutate(Bias = Estimate - True) %>%
+     ggplot(aes(x = Parameter, y = Bias, fill = Model)) +
+     geom_boxplot(position = position_dodge(width = 0.8)) +
+     theme_bw() +
+     geom_hline(yintercept = 0, color = "black", linetype = "dashed", linewidth = 0.7) +
+     ggtitle(paste("Correlation Parameter Bias (off-diagonals) -", label))
+   
+   # Arrange grid and save (bias-only)
+   grid_plot <- gridExtra::grid.arrange(
+     p_beta_bias, p_sigmae_bias, p_sigmaRE_bias, p_corr_bias,
+     ncol = 2
+   )
+   
+   ggsave(filename = paste0("BiasGrid_", label, ".png"),
+          plot = grid_plot, width = 10, height = 8)
+   
+   invisible(list(
+     results      = results,  # list of per-sim fits (packed)
+     store        = store,    # matrices for each method
+     beta_long    = beta_long,
+     sigmaRE_long = sigmaRE_long,
+     sigmae_long  = sigmae_long,
+     corr_long    = corr_long
+   ))
+ }
> 
> ## ------------------------------------------------
> ## 10. Missingness grid and SLURM array dispatch
> ## ------------------------------------------------
> 
> miss_grid <- data.frame(
+   setting  = paste0("miss_", 1:8),
+   p1_base  = seq(0.1, 0.2, length.out = 8),
+   p2_base  = seq(0.1, 0.4, length.out = 8),
+   p3_base  = seq(0.1, 0.6, length.out = 8)
+ ) %>% mutate(missing_base_avg = round((p1_base+ p2_base+ p3_base)/3,2))
> 
> # Get SLURM array index (default to 1 if not running under SLURM)
> task_id <- as.integer(Sys.getenv("SLURM_ARRAY_TASK_ID", "1"))
> 
> if (is.na(task_id) || task_id < 1 || task_id > nrow(miss_grid)) {
+   stop("Invalid SLURM_ARRAY_TASK_ID: ", task_id,
+        ". It must be between 1 and ", nrow(miss_grid), ".")
+ }
> 
> this_miss <- miss_grid[task_id, ]
> 
> label   <- this_miss$setting
> p1_base <- this_miss$p1_base
> p2_base <- this_miss$p2_base
> p3_base <- this_miss$p3_base
> 
> cat("### SLURM_ARRAY_TASK_ID =", task_id, "###\n")
### SLURM_ARRAY_TASK_ID = 3 ###
> cat("### Running missingness setting:", label, "\n")
### Running missingness setting: miss_3 
> 
> # Choose number of sims per setting
> N_sim <- 500
> 
> out_this <- run_sim_for_missing(
+   p1_base = p1_base,
+   p2_base = p2_base,
+   p3_base = p3_base,
+   N       = N_sim,
+   label   = label
+ )
=== Running simulation for miss_3 ===
   p1_base = 0.1285714 p2_base = 0.1857143 p3_base = 0.2428571 
   Fixed rho = ( -0.2 , 0.4 , 0.8 )
Using 45 cores for foreach.
No id variables; using all as measure variables
No id variables; using all as measure variables
No id variables; using all as measure variables
No id variables; using all as measure variables
No id variables; using all as measure variables
No id variables; using all as measure variables
No id variables; using all as measure variables
No id variables; using all as measure variables
No id variables; using all as measure variables
No id variables; using all as measure variables
No id variables; using all as measure variables
No id variables; using all as measure variables
No id variables; using all as measure variables
No id variables; using all as measure variables
No id variables; using all as measure variables
No id variables; using all as measure variables
> 
> # Save summary object per array (contains matrices + long dfs)
> saveRDS(out_this, file = paste0("AllOut_", label, ".rds"))
> 
> cat("Finished missingness setting:", label, "\n")
Finished missingness setting: miss_3 
> 
> proc.time()
    user   system  elapsed 
   3.457    0.249 7686.665 
